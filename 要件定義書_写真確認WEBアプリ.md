# 要件定義書：Instagram投稿確認WEBアプリ

**バージョン：** 2.2  
**作成日：** 2026-05-15  
**最終更新：** 2026-06-13  
**作成者：** 管理者

> **v2.2 改訂概要（2026-06-13 Instagram OAuth連携 実装完了）**
> - Instagram連携を **OAuth方式（Instagram API with Instagram Login）** に変更。Metaアプリは開発モードのまま運用し、連携アカウントを「Instagramテスター」に登録する方式（アプリ審査不要）
> - 連携フロー：管理画面でワンタイムリンク発行（30分有効・1回限り）→ クライアントがInstagramログイン → `/instagram/callback` → `instagramAccounts/{igUserId}` にトークン自動保存
> - トークンはアカウント別に `instagramAccounts` コレクションで管理。Cloud Function `refreshAccessTokens` が毎日自動リフレッシュ（旧 `systemConfig/instagram` との後方互換を維持）
> - 本番環境で実投稿まで動作確認済み（2026-06-13）
>
> **v2.1 改訂概要（2026-06-10 設計レビュー）**
> - 1案件＝1カルーセル投稿に統一（写真ごとのキャプション・投稿日時は廃止 → 投稿全体で共通）
> - 投稿予定枚数の最大上限を 30枚 → **10枚** に修正（Instagram Graph APIのカルーセル投稿上限）
> - LINE Notify は **2025年3月にサービス終了**のため、**LINE Messaging API** に変更
> - Fabric.js を廃止（プロトタイプで標準 Canvas API による実装を確認済み・ライブラリ不要）
> - 手書き書き込みを簡素化（丸・線の2種、色は赤/白の2色固定）
> - **確認者の複数名対応を追加**（URLアクセス時に名前を入力し、他の確認者の確認状況・フィードバックを閲覧できる）

---

## 1. プロジェクト概要

### 1.1 背景・目的

Instagram投稿代行サービスにおいて、投稿予定の写真・キャプション・ハッシュタグをクライアントが手軽に確認・フィードバックできる環境が必要。直感的に使えるWEBアプリで確認作業を効率化し、全写真のOKが揃った時点でInstagramへの予約投稿を自動実行することで、代行業務全体を自動化する。

### 1.2 サービス構造

```
管理者（代行業者・管理者 1名）
  │
  ├── クライアントAのInstagram Business Account へ代理投稿
  ├── クライアントBのInstagram Business Account へ代理投稿
  └── クライアントC…
```

- 投稿先は**クライアントのInstagram Businessアカウント**（業者自身のアカウントではない）
- 業者はMeta Developer登録済み
- クライアントごとに初回のみOAuth認証が必要

### 1.3 ゴール

| ゴール | 詳細 |
|--------|------|
| クライアントの確認作業を簡略化 | スマホで直感的に写真確認・フィードバックができる |
| 業者の投稿作業を自動化 | 全承認後の予約投稿を自動化してヒューマンエラーを防ぐ |
| やり取りの回数を削減 | 修正箇所が明確に伝わることで手戻りを減らす |

---

## 2. 利用者・利用環境

| 項目 | 内容 |
|------|------|
| 代行業者（管理者） | 管理者 1名のみ（マルチスタッフ機能は不要） |
| クライアント（確認者） | ITリテラシーの高くない50代女性 |
| 想定端末 | スマートフォン（iOS / Android） |
| 操作形態 | タッチ操作前提 |
| ネット環境 | 通常のWi-Fi / モバイル回線 |

---

## 3. 写真枚数の設計方針（重要）

### 3.1 可変設計を採用する

現在の運用は「投稿予定6枚＋差し替え候補2枚」だが、将来的に枚数を変更したいニーズがあるため、**最初から可変設計**で構築する。

1案件＝1カルーセル投稿（複数枚の写真を1つのInstagram投稿にまとめる形式）であり、キャプション・ハッシュタグ・投稿日時は案件（投稿）単位で1つとする。

| 項目 | デフォルト | 最大上限 | 最小 |
|------|-----------|---------|------|
| 投稿予定枚数 | 6枚 | 10枚（Graph APIカルーセル上限） | 1枚 |
| 差し替え候補枚数 | 2枚 | 10枚 | 0枚 |

- 案件作成時に業者が枚数を設定できる（変更しなければデフォルトの6＋2で動く）
- データベース・UI・投稿ロジックはすべて可変枚数に対応した設計にする
- **なぜ最初から可変にするか：** 後から変更しようとするとDB構造・画面・投稿ロジックを全面的に改修する必要があり、コストが大きい。最初に対応しておくとほぼ同じ工数で将来の拡張が無料になる

### 3.2 差し替え写真の投稿対象

- クライアントが「差し替えてください」と指示した場合、差し替え候補写真が投稿対象となる
- システムは差し替え指示を受けた時点で、投稿対象写真を自動的に候補写真に切り替える
- 差し替え後もキャプション・投稿日時は元の設定を引き継ぐ（業者が管理画面で変更可）

---

## 4. 機能要件

### 4.1 業者側（管理画面）

| # | 機能 | 詳細 |
|---|------|------|
| M-01 | クライアント管理 | クライアント情報（名前・Instagram連携状況）を登録・管理 |
| M-02 | Instagram連携（初回のみ） | OAuth認証でクライアントのInstagram Businessアカウントと連携 |
| M-03 | 案件作成 | クライアントを選択、投稿予定枚数・候補枚数（デフォルト6＋2）を設定して案件を作成 |
| M-04 | 写真アップロード | 投稿予定・差し替え候補を区別してアップロード。各写真に並び順を設定 |
| M-05 | キャプション入力 | 案件（1投稿）に共通のキャプション＋ハッシュタグを入力（文字数カウンター：キャプション2200字・ハッシュタグ30個） |
| M-06 | 投稿日時設定 | 案件（1投稿）に投稿日時を1つ設定（最大75日先まで） |
| M-07 | 確認URL発行 | クライアント専用URLを生成（有効期限7日） |
| M-08 | フィードバック確認 | OK/NG・コメント・書き込み画像・差し替え指示を写真ごとに一覧確認 |
| M-09 | ステータス管理 | 「確認待ち」「修正対応中」「全承認・投稿予約中」「投稿完了」を管理 |
| M-10 | 修正版再アップロード | NG写真を差し替えて同一URLで再確認を促す |
| M-11 | 予約投稿キャンセル・変更 | 自動投稿前に手動でキャンセル・日時変更できる |
| M-12 | 差し替え確認・承認 | クライアントの差し替え指示を確認し、投稿対象写真を確定する |

### 4.2 クライアント側（確認画面）

| # | 機能 | 詳細 |
|---|------|------|
| C-01 | 写真一覧表示 | 投稿予定写真をスクロールで確認（件数は可変、現在は6枚） |
| C-02 | 差し替え候補表示 | 差し替え候補を「候補」として明確に区別して表示 |
| C-03 | キャプション確認・修正 | 投稿共通のキャプション＋ハッシュタグを表示・テキスト直接修正可 |
| C-04 | 写真への手書き書き込み | 写真に指で丸・線を書き込める（色は赤/白の2色） |
| C-05 | OK / NG 判定 | 写真ごとに「OK」「NG（修正してください）」を選択 |
| C-06 | コメント入力 | 各写真・キャプションにテキストコメントを追加 |
| C-07 | 差し替え指示 | 投稿予定写真と差し替え候補を紐付けて「この写真に変えてください」と指示 |
| C-08 | 進捗インジケーター | 「全6枚中3枚確認済み」のように残り枚数がわかる表示 |
| C-09 | フィードバック送信 | 全写真確認後「送信する」ボタンで業者に一括通知 |
| C-10 | 送信前確認ダイアログ | 誤送信防止のため「この内容で送信しますか？」を表示 |
| C-11 | 完了メッセージ | 送信後に「ありがとうございます！確認しました😊」等を表示 |
| C-12 | 再確認対応 | 修正後に再度URLを開いて確認・再送信できる（2〜3回を想定） |
| C-13 | 確認者名の入力 | URLを開いた際に名前を入力（ログイン不要のまま「誰が確認したか」を記録） |
| C-14 | 確認状況の共有表示 | 同じURLで、ほかの確認者がすでに確認済みか・どんなフィードバック（OK/NG・コメント・差し替え指示）を送ったかを閲覧できる |

### 4.3 自動投稿機能

| # | 機能 | 詳細 |
|---|------|------|
| A-01 | 全承認検知 | 全投稿予定写真にOKが揃った瞬間に「全承認済み」へ自動移行 |
| A-02 | 差し替え処理 | 差し替え指示があった写真は投稿対象を候補写真に自動切り替え |
| A-03 | 業者へLINE通知 | 「〇〇様より全写真の承認をいただきました。予約投稿を開始します。」 |
| A-04 | Instagram自動投稿 | 設定日時にInstagram Graph APIでカルーセル投稿（キャプション・ハッシュタグ含む） |
| A-05 | 投稿完了通知 | 投稿完了時に業者の個人LINEへ通知 |
| A-06 | 投稿失敗アラート | APIエラー時は即座に業者へLINE通知（手動投稿を促す） |

### 4.4 LINE通知仕様（LINE Messaging API）

※ LINE Notifyは2025年3月にサービス終了したため、LINE公式アカウント経由のLINE Messaging API（Push通知）を使用する。

| タイミング | 通知内容 | 通知先 |
|-----------|----------|--------|
| フィードバック送信時 | 「〇〇様（確認者：△△様）からフィードバックが届きました。NGあり/全部OK」 | 業者の個人LINE |
| 全承認時 | 「〇〇様の全写真が承認されました。予約投稿をスタートします。」 | 業者の個人LINE |
| 投稿完了時 | 「〇〇様 〇月〇日分の投稿が完了しました。」 | 業者の個人LINE |
| 投稿失敗時 | 「⚠️ 投稿エラー：〇〇様 〇枚目 — 手動投稿をご確認ください。」 | 業者の個人LINE |
| トークン失効前 | 「⚠️ 〇〇様のInstagram連携が5日後に切れます。再認証をお願いします。」 | 業者の個人LINE |

---

## 5. Instagram代理投稿に関する技術的前提

### 5.1 初回セットアップフロー（クライアント1人につき1回）

```
1. 業者がMetaアプリ管理画面でクライアントのInstagramアカウントを「Instagramテスター」として招待
2. クライアントがInstagramアプリ（設定 → アプリとウェブサイト → テスター招待）で招待を承認
3. 業者が管理画面 → システム設定 → 「連携リンクを発行」
   （「ログイン画面を必ず表示する」オプションで force_reauth=true を付与可能）
4. 発行したリンク（有効期限30分・1回限り）をLINE等でクライアントへ送付
5. クライアントがリンクを開き、Instagram にログインして許可
6. /instagram/callback にリダイレクトされ、Cloud Function がトークン取得・保存
7. 管理画面の連携済み一覧に @ユーザー名 が表示されたら完了
8. 以降はシステムが自動で投稿・トークン更新を行う（Metaアプリ審査不要）
```

**使用API：** Instagram API with Instagram Login（instagram_business_basic / instagram_business_content_publish スコープ）

### 5.2 アクセストークン管理

| 項目 | 内容 |
|------|------|
| トークン種別 | Long-lived User Access Token（有効期限60日） |
| 保存場所 | Firestore `instagramAccounts/{igUserId}`（サーバーサイドのみ・管理者のみ読み書き可） |
| 取得方法 | OAuth連携フローで自動取得（手動貼り付けは旧方式として予備機能で残置） |
| 更新タイミング | 有効期限5日前にCloud Function `refreshAccessTokens` が毎日自動リフレッシュ |
| 複数アカウント対応 | 複数アカウントを連携でき、案件ごとに投稿先を指定できる（1件のみの場合は自動設定） |
| 失効時の対応 | 業者にアカウント名つきLINE通知 → 管理画面から再連携（連携リンクを再発行） |

### 5.3 Instagram Graph API制約

| 制約事項 | 内容 |
|----------|------|
| 1日あたりの投稿上限 | 25投稿まで |
| カルーセル投稿の枚数 | 1投稿あたり最大10枚 |
| 対応メディア | 画像（JPEG推奨）、動画（MP4） |
| キャプション上限 | 2,200文字（超過時に業者へ警告） |
| ハッシュタグ上限 | 30個まで（超過時に警告） |
| 予約投稿 | 最大75日先まで |

---

## 6. 非機能要件

### 6.1 ユーザビリティ（最重要）

- ボタンは大きく（最小48px以上）、タップしやすいデザイン
- 専門用語を使わない（「アップロード」→「写真を追加」等）
- 主要操作は最大3ステップ以内で完了できる流れ
- エラー時はわかりやすい日本語メッセージを表示
- 再確認時は前回のフィードバックが見えた状態でスタート

### 6.2 パフォーマンス

- 写真の表示は3秒以内（スマホ回線考慮）
- 写真は表示用に圧縮（最大1MB）、元データはStorageに保持

### 6.3 セキュリティ

- 確認URLはUUIDによる推測困難な文字列、有効期限7日
- クライアントはURLを知っていればログイン不要
- 業者管理画面はメール認証でログイン（1名運用のためシンプルな構成）
- Instagram APIトークンはFirestoreに保存、フロントエンドには非公開
- Cloud FunctionsのみがトークンにアクセスしてAPIを呼び出す

### 6.4 対応ブラウザ

- iOS Safari（最新2バージョン）
- Android Chrome（最新2バージョン）

---

## 7. 画面構成（画面フロー）

```
【業者：管理画面】
  ログイン（メール認証）
    ↓
  ダッシュボード（案件一覧・ステータス表示）
    ↓
  クライアント管理
    └── 初回のみ：Instagram連携 → OAuth認証 → トークン保存
    ↓
  案件作成
    ├── 投稿枚数設定（デフォルト：投稿6枚 + 候補2枚）
    ├── 写真アップロード（投稿予定・差し替え候補を区別）
    ├── キャプション・ハッシュタグ入力（1投稿で共通）
    └── 投稿日時設定（1投稿に1つ）
    ↓
  確認URL発行 → クライアントにLINE等で共有
    ↓
  フィードバック受信・確認
    ├── NGあり → 修正版アップロード → 同URLで再確認依頼
    ├── 差し替え指示あり → 投稿対象写真を確定
    └── 全承認 → 予約投稿スタート（自動）→ LINE通知

【クライアント：確認画面】
  URLを受け取る（LINEなど）
    ↓
  確認トップ画面（名前を入力 →「〇〇さん、今月の投稿をご確認ください」）
    └── 確認済みの確認者がいれば表示
    ↓
  写真一覧（投稿予定＋候補、進捗インジケーター付き）
    ├── 確認済みの確認者と各自のフィードバック内容を閲覧できる
    ↓
  各写真の詳細確認
    ├── 指で修正箇所を丸で囲む
    ├── OK / NG を選択
    ├── キャプション確認・テキスト修正
    ├── 差し替えたい場合は候補写真を選択
    └── コメントを一言入力
    ↓
  送信前確認ダイアログ → 「送信する」
    ↓
  完了画面（「ありがとうございます！確認しました😊」）

【システム：自動処理】
  全承認検知 → 業者にLINE通知
    ↓
  設定日時にCloud FunctionsがInstagram Graph APIで自動投稿
    ↓
  投稿完了 or 失敗 → 業者にLINE通知
```

---

## 8. データ構造（概要）

```
クライアント（Client）
  ├── ID, 名前
  ├── Instagram User ID
  └── アクセストークン（サーバーサイド保存）, 有効期限

案件（Project）＝ 1カルーセル投稿
  ├── ID, クライアントID, 作成日, ステータス
  ├── 確認URLトークン, 有効期限
  ├── キャプション, ハッシュタグ（1投稿で共通）
  ├── 投稿予定日時（1投稿に1つ）
  ├── Instagram投稿ステータス（未投稿 / 予約中 / 投稿済 / 失敗）
  ├── 投稿予定枚数（デフォルト6）, 候補枚数（デフォルト2）
  ├── 写真一覧（Photos[]：可変枚数）
  │     ├── ID, 画像URL, 種別（投稿予定 / 差し替え候補）, 表示順
  │     └── 実際の投稿対象フラグ（差し替え後に更新）
  └── フィードバック履歴（Feedback[]：確認者ごと・ラウンドごと）
        ├── 確認者名（URLアクセス時に入力）
        ├── 送信日時, ラウンド番号（1・2・3回目…）
        ├── 写真ごとの判定（OK / NG, 書き込み画像URL, 差し替え指示：候補写真ID）
        ├── キャプション修正テキスト
        └── 全体コメント
```

---

## 9. 技術スタック

| レイヤー | 採用技術 | 備考 |
|----------|----------|------|
| フロントエンド | React（Vite） | コンポーネント管理・スマホ最適化 |
| 手書き書き込み | Canvas API（標準） | プロトタイプで実装確認済み・外部ライブラリ不要 |
| バックエンド / DB | Firebase（Firestore + Storage + Auth） | サーバー不要・リアルタイム同期 |
| バックエンド処理 | Firebase Cloud Functions | 予約投稿実行・LINE通知・トークン更新 |
| ホスティング | Firebase Hosting | 同一環境でデプロイ可 |
| LINE通知 | LINE Messaging API | 業者の個人LINEへPush通知（LINE Notifyは2025年3月終了） |
| Instagram投稿 | Instagram Graph API | クライアントアカウントへの代理投稿（カルーセル） |

---

## 10. 開発フェーズ

| フェーズ | 内容 | 優先度 |
|----------|------|--------|
| Phase 1 | クライアント確認画面（写真表示・OK/NG・コメント・送信） | 最高 |
| Phase 2 | 手書き書き込み機能（Canvas API） | 高 |
| Phase 3 | 業者管理画面（アップロード・日時設定・URL発行・フィードバック確認） | 高 |
| Phase 4 | LINE Messaging API通知連携 | 高 |
| Phase 5 | Instagram Graph API連携・代理投稿・予約投稿自動化 | 高 ※API審査と並行 |
| Phase 6 | アクセストークン自動更新・失効通知 | 中 |
| Phase 7 | 差し替え処理・ステータス管理の精緻化 | 中 |

---

## 11. 未決事項

すべての質問が回答済みになりました。要件定義は**確定**です。

| # | 内容 | ステータス |
|---|------|-----------|
| Q1 | フィードバック通知方法 | ✅ LINE Notify（個人LINE） |
| Q2 | 複数回フィードバックの想定 | ✅ 複数回OK（2〜3回が最大） |
| Q3 | 写真枚数は固定か可変か | ✅ 可変設計（デフォルト6＋2、最大30＋10） |
| Q4 | InstagramはBusinessアカウントか / 代理投稿か | ✅ クライアントのBusiness Accountに代理投稿 |
| Q5 | Meta Developer App取得済みか | ✅ 登録済み |
| Q6 | LINE通知の届け先 | ✅ 業者の個人LINE（LINE Notify） |
| Q7 | 差し替え候補写真も投稿対象になるか | ✅ クライアントの差し替え指示があれば投稿対象 |
| Q8 | 複数スタッフで管理画面を使うか | ✅ 業者1名のみ |

---

## 12. 承認

| 役割 | 氏名 | 確認日 | サイン |
|------|------|--------|--------|
| 作成者 | 管理者 | 2026-05-15 | |

---

*要件定義フェーズ完了。次フェーズ：画面設計（ワイヤーフレーム）＆ DB詳細設計*
