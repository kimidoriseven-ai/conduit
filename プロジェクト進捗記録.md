# プロジェクト進捗記録
## Instagram投稿確認WEBアプリ

**記録日：** 2026年5月16日（最終更新：2026年6月13日）  
**プロジェクトオーナー：** 管理者（demo@example.com）

---

## プロジェクト概要

Instagram投稿代行サービスにおける、クライアントへの投稿確認・承認フローをWEBアプリで自動化するプロジェクト。全承認後はInstagram Graph APIで予約投稿まで自動実行する。

---

## 完了済み作業

### Phase 0 ── 要件定義（完了）

すべての要件が確定。未決事項なし。

### Phase 1 ── デザイン・設計（完了）

- ワイヤーフレーム 10画面（クライアント側6 ＋ 業者側4）をインタラクティブで作成・確認済み
- デザイン方向性：クリーン＆プロフェッショナル ＋ Instagramグラデーション（#833AB4 → #E1306C → #F77737）

### Phase 2 ── プロトタイプ開発（完了）

クライアント側・業者側の両プロトタイプをHTML単体ファイルで作成・動作確認済み。

---

## 成果物ファイル一覧

| ファイル名 | 内容 | 場所 |
|-----------|------|------|
| `要件定義書_写真確認WEBアプリ.md` | 確定版要件定義書（v2.0） | outputs/ |
| `photo-confirm-prototype.html` | クライアント側プロトタイプ | outputs/ |
| `admin-prototype.html` | 業者側管理画面プロトタイプ | outputs/ |
| `プロジェクト進捗記録.md` | 本ドキュメント | outputs/ |

---

## 確定済み要件サマリー

### サービス構造

```
管理者（代行業者・管理者1名）
  └── クライアントのInstagram Business Accountへ代理投稿
```

### 利用シーン

1件の投稿 = カルーセル形式の複数枚写真（最大30枚、デフォルト6枚）＋ 差し替え候補（デフォルト2枚）。キャプション・ハッシュタグは1投稿分で共通。

### 主要機能（確定）

**クライアント側：**
- URLを開いた際に確認者の名前を入力（ログイン不要のまま誰が確認したかを記録）
- ほかの確認者の確認状況（確認済みか・OK/NG件数・コメント）と、写真ごとのフィードバックを閲覧できる
- 縦スクロールで全写真を一覧確認（写真比率：4:5縦型）
- 写真ごとにOK / NG判定
- 写真への手書き書き込み（丸・線、赤/白）
- NG写真から差し替え候補を指定（候補A / B / 差し替えなし）
- キャプション・ハッシュタグを1回確認・編集
- 全写真確認後に一括送信 → 完了画面
- クライアント名の表示/非表示を業者側で切り替え可能

**業者側：**
- ログイン（メール認証）
- ダッシュボード：案件一覧・ステータス管理
- 案件作成ウィザード（3ステップ）
  1. 写真アップロード（投稿6枚 ＋ 候補2枚、実ファイル選択対応）
  2. キャプション・ハッシュタグ入力（文字数カウンター付き）
  3. 投稿日時設定（デフォルト：当日18:00）＋ 確認URL発行
- URL発行・コピー・LINEで送信
- フィードバック確認（確認者ごとにOK/NG・コメント・差し替え指示を写真単位で確認）

**自動化：**
- 全承認時に業者の個人LINEへ通知（LINE Messaging API）
- 設定日時にInstagram Graph APIで自動投稿
- 投稿完了・失敗をLINEで通知
- アクセストークンの自動リフレッシュ・失効通知

### 技術スタック（決定済み）

| レイヤー | 採用技術 |
|----------|----------|
| フロントエンド | React（Vite） |
| 手書き書き込み | Canvas API（標準・ライブラリ不要） |
| DB / Storage | Firebase（Firestore ＋ Storage） |
| 認証 | Firebase Auth |
| バックエンド処理 | Firebase Cloud Functions |
| ホスティング | Firebase Hosting |
| LINE通知 | LINE Messaging API（LINE Notifyは2025年3月終了） |
| Instagram投稿 | Instagram Graph API（代理投稿・カルーセル） |

### デザイン仕様

- グラデーション：`linear-gradient(135deg, #833AB4 0%, #E1306C 50%, #F77737 100%)`
- フォント：システムフォント（-apple-system / Hiragino Sans）
- 写真比率：4:5（Instagram縦型）
- ボタン：主要アクションはグラデーション、補助アクションはアウトライン
- 角丸：カード 18px、ボタン 28px（pill形状）

---

## プロトタイプ仕様メモ

### クライアント側（photo-confirm-prototype.html）

**画面構成（4画面）：**
1. ウェルカム画面 → 名前を入力（確認済みの確認者名も表示）→「確認をはじめる」
2. メインレビュー画面（縦スクロール）
   - 確認済みの方カード（確認者名・送信日時・OK/NG件数・全体コメント）
   - 進捗バー（X枚確認済み / 全6枚）
   - 写真カード × 6枚（4:5比率、書き込み・OK/NG・差し替え候補選択。NGやコメントが付いた写真には「ほかの確認者のフィードバック」を表示）
   - キャプション・ハッシュタグ（1回のみ）
   - 全体コメント欄
   - 送信ボタン（全確認後に活性化）
3. 送信確認ダイアログ
4. 完了画面（送信内容は確認状況に反映され、次の確認者から見える）

**データ構造（プロトタイプ内）：**
```js
photos[0..5] = { status:'none'|'ok'|'ng', drawing:null|dataURL, replace:null|-1|0|1 }
// replace: null=未選択, -1=差し替えなし, 0=候補A, 1=候補B
shared = { caption, hashtags, comment }
reviewerName = 'URLアクセス時に入力した名前'
prevFeedbacks[] = { reviewer, submittedAt, results[6], comment }  // 他の確認者の送信済みフィードバック
```

### 業者側（admin-prototype.html）

**画面構成（7画面）：**
1. ログイン
2. ダッシュボード（案件一覧・ステータス集計）
3. 案件作成 Step1：写真アップロード（File API対応・実写真プレビュー）
4. 案件作成 Step2：キャプション・ハッシュタグ
5. 案件作成 Step3：投稿日時（デフォルト18:00）＋ URL発行
6. 案件詳細
7. フィードバック確認

**デモデータ（プリセット）：**
- 田中様：確認待ち
- 佐藤様：フィードバック受信済み（確認者2名＝佐藤太郎様・佐藤花子様、NG2枚・コメント・差し替え指示あり）
- 山本様：投稿完了

---

## 2026年6月10日 更新（設計レビュー＋機能追加）

### 設計レビューで削除・修正した不要箇所

| 項目 | 変更内容 | 理由 |
|------|----------|------|
| 写真ごとのキャプション・投稿日時 | 廃止 → 1案件＝1カルーセル投稿で共通に統一 | 確定済みのカルーセル方式と要件定義書v2.0の記述が矛盾していた |
| 投稿予定枚数の上限30枚 | 10枚に修正 | Instagram Graph APIのカルーセル投稿上限が10枚のため実現不可能だった |
| Fabric.js | 採用取りやめ（標準Canvas APIに変更） | プロトタイプで標準Canvas APIのみで実装済み。外部ライブラリは不要 |
| LINE Notify | LINE Messaging APIに変更 | LINE Notifyは2025年3月にサービス終了済み |
| 手書きの色・太さ選択 | 丸・線2種、赤/白2色固定に簡素化 | プロトタイプで確定したUIに合わせた（高齢クライアント向けに選択肢を減らす） |
| プロトタイプ内の未使用コード | 削除 | 未使用CSSクラス（.card / .b-none / .btn-sm / .step-bar系）、未使用変数・関数（COLORS / goLogin / hasFb）など |
| ファイル名 `PHOTO-~1.HTM` | `photo-confirm-prototype.html` にリネーム | 8.3形式に壊れていたファイル名を修正 |

### 追加機能：確認者の複数名対応（C-13 / C-14）

- 確認URLを開いた際に確認者が名前を入力（ログイン不要のまま記録）
- ウェルカム画面・レビュー画面で「誰が確認済みか」を表示
- 各写真の下に、ほかの確認者のフィードバック（OK/NG・差し替え指示・コメント）を表示
- 送信した内容は確認状況に反映され、後から開いた確認者に共有される
- 業者側：フィードバックを確認者ごとに表示。案件詳細の写真バッジは「誰か1人でもNGならNG」で集計
- 要件定義書をv2.1に改訂（C-13・C-14追加、Feedbackに確認者名を追加）

---

## 2026年6月13日 更新（Instagram OAuth連携）

### 実装内容

| 項目 | 内容 |
|------|------|
| 連携方式の変更 | トークン手動貼り付け → Instagram API with Instagram Login による OAuth方式 |
| Metaアプリ運用方針 | **開発モードのまま**（連携アカウントを「Instagramテスター」に登録する方式・アプリ審査不要） |
| 新規 Cloud Functions | `createInstagramConnectLink`（管理者専用 Callable・ワンタイムstate発行）、`completeInstagramOAuth`（公開 Callable・stateトランザクション検証・code→長期トークン交換） |
| 変更 Cloud Functions | `executeInstagramPost`：投稿先解決を3段階に変更・投稿APIをフォーム形式に修正（JSONボディは OAuthException code 2 の原因）、`refreshAccessTokens`：旧設定＋`instagramAccounts`全件をリフレッシュ対象に、`testInstagramPost`：失敗時Metaエラーを管理画面に表示 |
| 新規 Firestore コレクション | `instagramAccounts/{igUserId}`（トークン・ユーザー名を管理者のみ読み書き）、`oauthStates/{state}`（Admin SDKのみ・クライアントアクセス全拒否） |
| projects への追加 | 任意フィールド `instagramAccountId`（2件以上連携時に案件ごとの投稿先を指定） |
| 管理画面 | 案件作成画面に連携アカウント2件以上のとき投稿先セレクトを表示。旧「Instagram手動設定」は予備として残置 |

### 投稿成功確認

2026年6月13日、本番環境でテスト投稿が成功するまで動作確認済み。

### 実装体制

- 設計・コード監査：Claude メインセッション（Fable 5）
- コード実装：Claude Opus（Functions）/ Sonnet（フロントエンド・ドキュメント）サブエージェント分担
- Metaダッシュボード設定・動作検証：管理者

---

## フェーズ進捗（2026年6月13日時点）

### Phase 3 ── Firebase構築（✅ 完了）

Firestore・Storage・Auth・Hosting・Cloud Functions・LINE Messaging API・Instagram OAuth連携・トークン自動更新まですべて本番稼働中。

### Phase 4 ── テスト・本番リリース（おおむね完了）

- ✅ Firebase Hosting デプロイ（https://conduit-app.com）
- ✅ LINE Messaging API 本番設定
- ✅ Instagram 実投稿の動作確認（2026-06-13）
- ✅ ポートフォリオ公開（公開リポジトリ・ライブデモ）
- 残：実クライアントでの本格運用と、不特定多数に開放する場合のMetaアプリ審査（現在はテスター方式のため不要）

---

*最終更新：2026年6月13日*
